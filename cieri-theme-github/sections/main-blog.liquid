<div class="legal-page"><h1 class="legal-title">{{ blog.title }}</h1>{% for article in blog.articles %}<h2><a href="{{ article.url }}">{{ article.title }}</a></h2>{% endfor %}</div>

{% schema %}
{
  "name": "Blog",
  "settings": [],
  "presets": [
    {
      "name": "Blog"
    }
  ]
}
{% endschema %}
